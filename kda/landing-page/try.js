

// let s ='adjfeuddwwrwdahsisvasfa'.split('')

// //Print all without repetition
// console.log((s.filter((e,i)=> s.indexOf(e)== i )).join(''))
// //Print all numnber that doesn't repeat
// console.log((s.filter((e,i)=> s.indexOf(e)== s.lastIndexOf(e))).join(''))

// let obj = { '1':'2','3':'4'}


// console.log(Object.entries(obj))



n=2
s='Hello world !'
let ok = (_, b)=>Array.from({length:Math.ceil(s.length/n)},(_,i)=>s[i*n+b]||'x').join``
print(Array.from({length:+n},(_, b)=>Array.from({length:Math.ceil(s.length/n)},(_,i)=>s[i*n+b]||'x').join``).join` `)


